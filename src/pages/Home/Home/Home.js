import React from 'react';
import Banner from '../Banner/Banner';
import Unique from '../Unique/Unique';

const Home = () => {
    return (
        <div>
            <Banner></Banner>
            <Unique></Unique>
        </div>
    );
};

export default Home;